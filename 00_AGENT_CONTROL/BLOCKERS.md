# Blockers Log

`AGENT_MASTER_INSTRUCTIONS.md` ("If blocked") requires every genuine blocker to be recorded
here before asking the human — never worked around silently, never guessed past.

Use [`../03_TEMPLATES/BLOCKER_TEMPLATE.md`](../03_TEMPLATES/BLOCKER_TEMPLATE.md) for each
new entry. Append new entries below; do not delete resolved ones — mark them `RESOLVED`
with a one-line note on how, so this file stays a real history of decisions that shaped the
build, not just an inbox.

---

No blockers logged yet.
