"""Baseline machine-learning engine: task detection, target validation, preprocessing,
training, evaluation, and model comparison.

Scope, per `01_PHASES/PHASE_04_ML_ENGINE/PHASE_PROMPT.md`: transparent, reproducible,
deterministic baselines only — no AutoML, no hyperparameter search, no model
serving/persistence infrastructure, no AI-generated commentary (that is Phase 05's job,
narrating this module's already-computed metrics, never altering or inventing them). A
model's performance is reported, never characterized as "good" or "bad" by this layer.
"""
